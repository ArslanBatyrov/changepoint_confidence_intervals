import(methods)
import(graphics)
import(zoo)
import(stats)

export("cpt.mean", "cpt.meanvar", "cpt.var","cpt.reg")
export("BINSEG", "decision", "penalty_decision","PELT","class_input")
export("confidence_set")
exportClasses(cpt,cpt.reg,cpt.range)
exportMethods(plot,summary,show,nseg,logLik,likelihood,fitted,residuals,data.set,data.set.ts,cpts,cpts.full, cpts.ts,cpttype,distribution,test.stat,
	method,ncpts,ncpts.max,param.est,param, coef,confint,pen.type,pen.value,pen.value.full, minseglen, seg.len,conf.set,"cpts<-","cpttype<-","data.set<-","distribution<-","test.stat<-","ncpts.max<-","param.est<-","pen.type<-",
	"pen.value<-", "method<-", "cpts.full<-", "minseglen<-","pen.value.full<-","conf.set<-")

S3method(print, cpt.confset)
S3method(print, cpt.confsets)

useDynLib(changepoint, .registration=TRUE)
